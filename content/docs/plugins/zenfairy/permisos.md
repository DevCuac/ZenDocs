---
title: "Permisos"
description: "Nodos de permiso disponibles en ZenFairy."
---

- `zenfairy.command.use` — Usar comandos de jugador (default: `true`).
- `zenfairy.admin` — Acceso a comandos administrativos (default: `op`).
- `zenfairy.admin.reset` — Permite `/zf reset` (default: `op`).
